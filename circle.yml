dependencies:
  cache:
    - "~/dcmqi-build/ITK"
    - "~/dcmqi-build/DCMTK"
  pre:
    - sudo apt-get update; sudo apt-get install cmake
  post:
    - mkdir ~/dcmqi-build && cd ~/dcmqi-build && cmake ../dcmqi && make
